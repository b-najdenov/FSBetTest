@model IEnumerable<FSBetTest.Models.Game>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GameID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TeamA.TeamName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TeamB.TeamName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Outcome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GameID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TeamA.TeamName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TeamB.TeamName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Outcome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GameID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GameID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GameID })
        </td>
    </tr>
}

</table>
